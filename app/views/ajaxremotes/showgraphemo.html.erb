<%= line_chart @graphdata,adapter: "highcharts",library: {
                    yAxis: {
                        labels: {
                            enabled: true,
                        },
                        min: 1, max: 3, tickInterval: 1
                    }
                } %>