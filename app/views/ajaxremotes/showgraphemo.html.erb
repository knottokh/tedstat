<%= column_chart @graphdata,adapter: "highcharts",library: {
                    yAxis: {
                        labels: {
                            enabled: true,
                        },
                        min: 0, max: 3, tickInterval: 1
                    }
                } %>