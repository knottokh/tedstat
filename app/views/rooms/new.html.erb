<%= simple_form_for(@room, remote: request.xhr?, html: { data: { modal: true,navigate: false,afterfunction: "updateroom" } },validate: true) do |f| %>
    <%= f.error_notification %>
    <%= render partial: "form" , locals: {f: f,courses: @courses}%>
<% end %>